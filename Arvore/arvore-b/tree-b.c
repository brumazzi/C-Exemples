#include <tree-b.h>

void tree_insert(Tree t, long long int index, void *data){
}

struct *tree_search(Tree t, long long int index){
	int x;
	struct node *node = *t;
	for(x=0; x<node->elements; x++){
		struct item *buff;
		buff = node
		if(node->
	}
}
